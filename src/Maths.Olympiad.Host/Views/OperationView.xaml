<UserControl x:Class="Maths.Olympiad.Host.Views.OperationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:converters="clr-namespace:Maths.Olympiad.Host.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <converters:BooleanNotConverter x:Key="BooleanNotConverter" />
    </UserControl.Resources>
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <ContentControl  Grid.Row="0" Content="{Binding TestSessionInput}" IsEnabled="{Binding InProgress, Converter={StaticResource BooleanNotConverter}}" />

        <Button Grid.Row="1" Style="{StaticResource ActionButtonStyle}" Click="ButtonBase_OnClick" Command="{Binding GenerateCommand}" Content="Generate" IsEnabled="{Binding InProgress, Converter={StaticResource BooleanNotConverter}}" />

        <ContentControl  Grid.Row="2" Content="{Binding Session}"/>
        
    </Grid>
</UserControl>
